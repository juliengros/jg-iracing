# StreamElements Widgets

Shared workspace for developing StreamElements widgets used in the stream.

## Structure

- `assets/` - shared brand assets reused across widgets.
- `widgets/` - individual widgets and their local preview files.
- Each widget should stay self-contained so it can be copied into StreamElements easily.

## First widget

- `widgets/follow-alert/` - follow alert prototype with a local preview page.
